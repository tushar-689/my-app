import { PlaceholderScreen } from '@/features/shared/screens/placeholder-screen';

export default function ExamRoute() {
  return (
    <PlaceholderScreen
      eyebrow="EXAM MODE"
      title="Your exam room is almost ready."
      body="Timed full-length mocks will live here in the next prototype milestone."
      color="yellow"
    />
  );
}
